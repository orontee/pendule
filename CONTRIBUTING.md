# Contributing to pendule

## Screen captures

First, on must enable debug support in Weston: Add `--debug` on
`weston` command line. Then run:

``` bash
$ export WAYLAND_DISPLAY=wayland-1
$ weston-screenshooter shoot done
```
